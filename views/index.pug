extends layout

block content
  h1= title
  p Welcome to #{title}
  a(href='/categories') See all categories
  div.items-container
    ul
      each item in itemsList
        li
          a(href=item.url) #{item.name}

